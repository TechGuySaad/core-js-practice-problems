console.log(x);

var x = 5;

// Above code will output undefined because hoisting can be done on var, what happens is the declaration will be moved to the top but
// the initialization will not be moved to the top
// the code will work but as x will be empty so this will output undefined
